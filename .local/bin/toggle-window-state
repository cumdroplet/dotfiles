#!/bin/sh

state() {
	bspc node focused -t "$1"
	printf "Toggled window state: %s\n" "$1"
}

case $(bspc query -T -n | jq -r ".client.state") in
	"tiled")
		state fullscreen
		;;
	"fullscreen")
		state tiled
esac
